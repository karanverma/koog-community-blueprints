#!/usr/bin/env sh

if [ -n "$JAVA_HOME"  ] ; then
  JAVACMD="$JAVA_HOME/bin/java"
else
  JAVACMD="java"
fi

exec "$JAVACMD" -classpath "$(dirname "$0")/../03-guarded-tool-agent/gradle/wrapper/gradle-wrapper.jar" org.gradle.wrapper.GradleWrapperMain "$@"
